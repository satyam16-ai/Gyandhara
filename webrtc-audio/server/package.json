{
  "name": "webrtc-audio-sfu-server",
  "version": "1.0.0",
  "description": "WebRTC SFU server for audio streaming in classroom",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install --production=false",
    "postinstall": "echo 'Post-install completed'",
    "test": "echo 'No tests specified'",
    "health": "wget --no-verbose --tries=1 --spider http://localhost:$PORT/health || curl -f http://localhost:$PORT/health || exit 1",
    "prestart": "echo 'Starting Gyandhara Audio SFU Server...'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "mediasoup": "^3.12.16",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "winston": "^3.10.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  },
  "keywords": [
    "webrtc",
    "sfu",
    "audio",
    "streaming",
    "classroom",
    "mediasoup",
    "render"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/satyam16-ai/Gyandhara-SIH-project"
  },
  "license": "MIT",
  "author": "Gyandhara Team"
}